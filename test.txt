function convertAmountToWords() {
    // Input fields for currency and amount
    let currency = document.getElementById("LC_CCY").value.trim().toUpperCase();
    let amount = parseFloat(document.getElementById("LC_AMT").value);

    // Basic validation
    if (isNaN(amount) || amount < 0) {
        alert("Please enter a valid amount");
        return;
    }

    // Function to convert numbers to words
    function numberToWords(num) {
        const belowTwenty = ["", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine", "Ten", "Eleven", "Twelve", "Thirteen", "Fourteen", "Fifteen", "Sixteen", "Seventeen", "Eighteen", "Nineteen"];
        const tens = ["", "", "Twenty", "Thirty", "Forty", "Fifty", "Sixty", "Seventy", "Eighty", "Ninety"];
        const aboveThousand = ["", "Thousand", "Million", "Billion", "Trillion"];

        if (num === 0) return "Zero";
        
        let words = "";

        // Helper function to handle chunks of 3 digits
        function convertHundred(num) {
            let result = "";
            if (num > 99) {
                result += belowTwenty[Math.floor(num / 100)] + " Hundred ";
                num %= 100;
            }
            if (num > 19) {
                result += tens[Math.floor(num / 10)] + " ";
                num %= 10;
            }
            if (num > 0) {
                result += belowTwenty[num] + " ";
            }
            return result.trim();
        }

        let index = 0;
        while (num > 0) {
            if (num % 1000 !== 0) {
                words = convertHundred(num % 1000) + " " + aboveThousand[index] + " " + words;
            }
            num = Math.floor(num / 1000);
            index++;
        }

        return words.trim();
    }

    // Get integer and decimal parts of the amount
    const integerPart = Math.floor(amount);
    const decimalPart = Math.round((amount - integerPart) * 100);

    // Convert integer part and decimal part to words
    let words = `${numberToWords(integerPart)} ${currency}`;
    if (decimalPart > 0) {
        words += ` and ${numberToWords(decimalPart)} Cents`;
    }

    // Display the result
    document.getElementById("amountInWords").innerText = words;
}

// Example usage:
// Add an HTML button to trigger this function and a span to display the result

<input type="text" id="LC_CCY" placeholder="Enter Currency (e.g., USD)">
<input type="number" id="LC_AMT" placeholder="Enter Amount">
<button onclick="convertAmountToWords()">Convert to Words</button>
<p id="amountInWords"></p>