String identifier = pr.getProperty("filename");
      this.list = sftpChannel.ls("*.xml");
      logg.info("list Value : [" + this.list + "] sftpChannel : [" + sftpChannel + "]");
	  
	  
	  
	  
in properties file the filename is "_signed" 
the files ends with the identifier only to be taken into the list.
