Error starting at line 1 in command:
DECLARE
    input_query    VARCHAR2(1000) := 'SELECT FORM_OF_LC FORM_OF_LC, APPL_ID APPL_ID, APPL_NM APPL_NM, APPL_ADD APPL_ADD, BENE_NM_ADD BENE_NM_ADD, NEW_LC_AMT LC_AMT, TOLERANCE_MINUS TOLERANCE_MINUS, TOLERANCE_PLUS TOLERANCE_PLUS, CONF_INSTR_FLG CONF_INSTR_FLG, ISSUE_DT ISSUE_DT, EXPIRY_DT EXPIRY_DT, EXPIRY_PLC EXPIRY_PLC, NEW_EXPIRY_DT NEW_EXPIRY_DT, TENOR_DAYS TENOR_DAYS, TENOR_TYPE TENOR_TYPE, PARTIAL_SHIP PARTIAL_SHIP, TNSHIP TNSHIP, SHIP_FROM SHIP_FROM, PORT_OF_DELV PORT_OF_DELV, LTST_SHIP_DT LTST_SHIP_DT, GOODS_DESC GOODS_DESC, ADDIT_CONDITION ADDIT_CONDITION, DETRMNTL_FLG DETRMNTL_FLG, NEW_LC_AMT NEW_LC_AMT, DRAFTS_AT DRAFTS_AT, INCOTERMS INCOTERMS, LC_CCY LC_CCY, USANCE_FROM USANCE_FROM, FREIGHT FREIGHT, DRAFT_REQ DRAFT_REQ, INT_RATE INT_RATE, DRAFT_REPNT DRAFT_REPNT, NEW_LTST_SHIP_DT NEW_LTST_SHIP_DT, BENE_CONS_DT BENE_CONSENT_DT, AMD_NO NO_OF_AMD, AMD_DT AMD_DT, INIS_Amend_EE2CE MSG_TYPE, INIS CSX_MODULE, ETRADE_REF_NO C_MAIN_REF, ISSUED_BY ISSUED_BY, AMD_DETL AMD_DETAILS, AVAL_BY AVAL_BY, BENE_CONS_FLG BENE_CONS_FLG, LC_LIAB_TYPE LC_LIAB_TYPE, LC_REFNO LC_NO, LC_BAL_PRSNT_CE LC_BAL_PRSNT, NEW_APPL_NM_ADD NEW_APPL_NM_ADD, NEW_APPL_ID NEW_APPL_NM, NOTES NOTES, ORGI_BR_CODE ORGI_BR_CODE, NEED_CPC_CODE NEED_CPC_CODE, NXT_STATUS NXT_STATUS, DRFT_FR_INT_REQ STATUS, NEW_LC_BAL NEW_LC_BALA, INC_AMT INC_LC_AMT_BY, DEC_AMT DEC_AMT_BY, ORGI_BR_CODE PROC_BR_CODE, NEW_APPL_NM_ADD NEW_APPL_ADD, PORT_OF_CODE PORT_OF_SHIP_CODE, PORT_OF_DELV SHIP_TO, BENE_ID BENE_ID, NEW_BENE_NM_AND_ADD NEW_BENE_NM_ADD, TEMP1 TEMP1, NEW_LC_BAL LC_BALA, USANCE_FROM TENOR_FROM, NEW_LOAD_PORT NEW_LOAD_PORT, NEW_PORT_OF_DISCHARGE NEW_PORT_OF_DISCHARGE, NEW_LOAD_ON_BORD NEW_LOAD_ON_BORD, NEW_TRANSPORTATION_TO NEW_TRANSPORTATION_TO, NEW_EXPIRY_PLC NEW_EXPIRY_PLC, NEW_AVAL_WT_BK_ID NEW_AVAL_WT_BK_ID, NEW_AVAL_WT_BK_NM_AND_ADD NEW_AVAL_WT_BK_NM_AND_ADD, NEW_PARTIAL_SHIP NEW_PARTIAL_SHIP, NEW_TNSHIP NEW_TNSHIP, NEW_TENOR_TYPE NEW_TENOR_TYPE, NEW_TENOR_DAYS NEW_TENOR_DAYS, NEW_USANCE_FROM NEW_USANCE_FROM, NEW_GOODS_DESC NEW_GOODS_DESC, NEW_DOC_REQ NEW_DOC_REQ, NEW_ADDIT_CONDITION NEW_ADDIT_CONDITION, NEW_INSURANCE_BY NEW_INSURANCE_BY, NEW_INCOTERMS NEW_INCOTERMS, INCOTERMS_YR_TEMP INCOTERMS_YR_TEMP, NEW_CONF_BK_ID NEW_CONF_BK_ID, CONF_BK_ADD_SEND_CE CONF_BK_ADD_SEND_CE, TRX_REF_AML TRX_REF_AML, NEW_AVAL_BY NEW_AVAL_BY, NEW_INSU_POLICY_NO NEW_INSU_POLICY_NO, NEW_CONSIGNEE_NM_ADD NEW_CONSIGNEE_NM_ADD,  SYS_IMG_TYPE, T_GOOD_CODE_CONCAT T_GOOD_CODE_CONCAT, T_GOOD_DESC_CONCAT T_GOOD_DESC_CONCAT, T_GOOD_ISALLOW_CONCAT T_GOOD_ISALLOW_CONCAT, T_GOOD_TYPE_CONCAT T_GOOD_TYPE_CONCAT, T_GOOD_REASON_CONCAT T_GOOD_REASON_CONCAT, NEW_BENE_ACC_NO NEW_BENE_ACC_NO, NEW_BENE_IFSC_CODE NEW_BENE_IFSC_CODE, NEW_AVAIL_CONTCT NEW_AVAIL_CONTCT, NEW_BENE_BK_NM NEW_BENE_BK_NM, NEW_BENE_BK_ADD NEW_BENE_BK_ADD, NEW_ORIG_CONTRACT_VAL NEW_ORIG_CONTRACT_VAL FROM INLC_MASTER;';
    table_name     VARCHAR2(100) := 'INLC_MASTER';
    valid_query    VARCHAR2(1000);
    col_name       VARCHAR2(100);
    alias_name     VARCHAR2(100);
    col_list       VARCHAR2(1000);
    token          VARCHAR2(1000);
    pos            NUMBER;
    column_exists  BOOLEAN;

BEGIN
    -- Initialize valid_query with the starting portion of the select
    valid_query := 'SELECT ';

    -- Extract each column in the select statement from the input query
    input_query := REGEXP_REPLACE(input_query, '.*SELECT\s+', '', 1, 1, 'i');
    input_query := REGEXP_REPLACE(input_query, 'FROM.*', '');

    -- Loop through each column in the input_query
    LOOP
        -- Find the next comma
        pos := INSTR(input_query, ',');

        -- If no more commas, get the remaining part
        IF pos = 0 THEN
            token := TRIM(input_query);
        ELSE
            token := TRIM(SUBSTR(input_query, 1, pos - 1));
            input_query := SUBSTR(input_query, pos + 1);
        END IF;

        -- Extract column name and alias, if any
        col_name := REGEXP_SUBSTR(token, '^[^ ]+');
        alias_name := REGEXP_SUBSTR(token, '[^ ]+$');
        IF alias_name = col_name THEN
            alias_name := NULL; -- No alias
        END IF;

        -- Check if column exists in the table
        SELECT COUNT(*) INTO column_exists
        FROM ALL_TAB_COLUMNS
        WHERE TABLE_NAME = table_name
        AND COLUMN_NAME = col_name;

        -- If column exists, add it to the valid_query
        IF column_exists THEN
            IF alias_name IS NOT NULL THEN
                col_list := col_list || ', ' || col_name || ' ' || alias_name;
            ELSE
                col_list := col_list || ', ' || col_name;
            END IF;
        END IF;

        -- Exit the loop if we processed the last column
        EXIT WHEN pos = 0;
    END LOOP;

    -- Remove the leading comma and add the FROM clause
    valid_query := valid_query || SUBSTR(col_list, 3) || ' FROM ' || table_name;

    -- Output the valid query
    DBMS_OUTPUT.PUT_LINE(valid_query);
END;
Error report:
ORA-06550: line 41, column 30:
PLS-00382: expression is of wrong type
06550. 00000 -  "line %s, column %s:\n%s"
*Cause:    Usually a PL/SQL compilation error.
*Action:
